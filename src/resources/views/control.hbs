<!DOCTYPE html>
<html>

<head>
</head>

<body>
    <div class="mt-4"></div>
    <h1 style="color: red">Điều khiển các thiết bị</h1> 
    {{#each devices}}

    <div class="mt-4">

        <div class="row">

            <div class="mt-ios">
                <h1>
                    <a class="{{this.type}}" href="/control/{{this._id}}" role="button">{{this.Show}}</a>
                    {{this.Name}}
                </h1>
                
            </div>
        </div>
        <div class="mt-4"></div>
    </div>
    {{/each}}