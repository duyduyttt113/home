<!DOCTYPE html>
<html>

<head>
</head>

<body>
    <div class="mt-4"></div>
    <div class="col d-flex justify-content-center">
    <h1 style="color: orange">Điều khiển thiết bị</h1> 
    </div>
    {{#each devices}}

    <div class="mt-4">

        <div class="row">

            <div class="mt-ios">
                <h1>
                    <a class="{{this.type}}" href="/control/{{this._id}}" role="button">{{this.Show}}</a>
                    {{this.Name}}
                </h1>
                
            </div>
        </div>
        <div class="mt-4"></div>
    </div>
    {{/each}}